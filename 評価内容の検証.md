````AI1
UTF-8指定で `評価内容.md` と該当コードを確認しました。結論としては、指摘の大半は妥当です。ただし、いくつかは「指摘の方向は合っているが、例や理由が不正確」「脅威モデル限定」「すでにコード上は対策済み」でした。

**優先度高め**
| # | 判定 | 裏取り |
|---|---|---|
| 1 | 一部妥当 | `playlistFolder = Path.Combine(...)` は実在します。ただし `/` と `\` は Windows では無効文字として置換されるため、`"../.."` 例は不正確です。一方で `".."` は残るため親ディレクトリに解決されます。根本指摘は妥当。 [DownloadViewModel.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/ViewModels/DownloadViewModel.cs:301>) |
| 2 | 妥当 | `PlayFile` は `LocalFilePath` を `UseShellExecute=true` で開きます。`ExternalLinkOpener` も URL スキーム検証なしで開きます。ただし実害は metadata 改ざんや yt-dlp 出力信頼が前提です。 [LibraryViewModel.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/ViewModels/LibraryViewModel.cs:221>) |
| 3 | 妥当 | `explorer.exe` の `Arguments` を文字列結合しています。通常の実ファイルパスに `"` は入りませんが、metadata 改ざん前提なら崩せます。`ArgumentList` 化が安全です。 [LibraryViewModel.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/ViewModels/LibraryViewModel.cs:205>) |
| 4 | 妥当 | 失敗通知後、`finally` で `_cancellationTokens.Remove(job.Id)` が無条件実行されます。Retry が先に新 CTS を入れると消されるレースは構造上あり得ます。 [DownloadManager.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/Services/DownloadManager.cs:225>) |
| 5 | 一部妥当 | `AbsorbPermitAsync()` は fire-and-forget です。ただし中身は `WaitAsync()` で、評価文の `SemaphoreFullException` 例はこの経路では通常出ません。監視不能な非同期処理という指摘だけ妥当。 [DownloadManager.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/Services/DownloadManager.cs:112>) |

**リソース系**
| # | 判定 | 裏取り |
|---|---|---|
| 6 | 妥当 | `static Dictionary<string, BitmapImage>` に削除処理なし。URL数に比例して増えます。 [ThumbnailImageConverter.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/Infrastructure/ThumbnailImageConverter.cs:20>) |
| 7 | 妥当 | `_cache` に全件保持し、保存ごとに全件 JSON シリアライズして `metadata.json` を書き換えます。 [MetadataRepository.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/Services/MetadataRepository.cs:66>) |
| 8 | 妥当 | `_allJobs` は `ClearCompleted()` / `ClearAll()` まで保持され、`GetAllJobs()` は配列コピーします。 [DownloadManager.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/Services/DownloadManager.cs:39>) |
| 9 | 妥当 | `DownloadViewModel` がイベント購読しますが解除なし。現状は `MainWindowViewModel` がキャッシュするので顕在化しにくい、という評価も合っています。 [DownloadViewModel.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/ViewModels/DownloadViewModel.cs:44>) |

**yt-dlp 連携**
| # | 判定 | 裏取り |
|---|---|---|
| 10 | 妥当 | `--force-overwrites` が常に付与されています。同名出力は上書きされ得ます。 [YtDlpArgumentBuilder.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/Services/YtDlp/YtDlpArgumentBuilder.cs:51>) |
| 11 | 妥当 | User-Agent は `Chrome/120.0.0.0` 固定です。古い固定 UA という指摘は妥当。 [YtDlpArgumentBuilder.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/Services/YtDlp/YtDlpArgumentBuilder.cs:143>) |
| 12 | 妥当 | `url.Contains("list=") || url.Contains("/playlist")` だけでプレイリスト扱いになります。`watch?v=...&list=...` の期待ズレは起き得ます。 [YtDlpAnalyzer.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/Services/YtDlp/YtDlpAnalyzer.cs:31>) |
| 13 | 妥当 | stderr は現在カルチャの ANSI コードページ固定で読んでいます。Python/環境変数次第で UTF-8 になる可能性を考えると指摘は妥当。 [YtDlpProcessRunner.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/Services/YtDlp/YtDlpProcessRunner.cs:78>) |

**機能不足系**
#14〜#17、#19〜#21 は妥当です。キュー永続化、Cookie/proxy、字幕、重複 URL チェック、バッチ入力、速度制限、プレイリスト項目ごとの品質指定は、設定モデル・UI・yt-dlp 引数のいずれにも該当実装が見当たりませんでした。

#18 は一部妥当です。自動で無限リトライする実装ではありませんが、手動 Retry の回数制限はありません。 [DownloadManager.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/Services/DownloadManager.cs:293>)

**その他**
`SettingsRepository.Load()` の同期 I/O 指摘は妥当です。 [SettingsRepository.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/Services/SettingsRepository.cs:43>)

`AtomicFileWriter` の `directory ?? string.Empty` は実在しますが、「ルートディレクトリに作る」は不正確で、相対パスならカレントディレクトリになります。現状の呼び出しは AppData 配下の絶対パスなので実害は薄いです。 [AtomicFileWriter.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/Services/AtomicFileWriter.cs:17>)

`CreateNoWindow` 未確認の指摘は不成立です。現在コードでは `CreateNoWindow = true` が設定済みです。 [YtDlpProcessRunner.cs](<C:/Users/Rakua/Documents/VScode/C#/YT-Downloader_GUI/YouTubeDownloader/Services/YtDlp/YtDlpProcessRunner.cs:60>)
````

````
評価内容.mdとリポジトリ構成を確認します。

評価対象のコードを順に確認します。関連ファイルを一括で読み込みます。

次に並行性・リソース・yt-dlp連携関連のファイルを確認します。

yt-dlp連携・引数生成・プロセス起動・設定まわりを確認します。

残りの確認点（プレイリストURL判定、MainWindowViewModelのインスタンス管理、ダウンロード処理本体）を確認します。

残りの機能関連（Cookie/プロキシ/字幕/レート制限の有無）を一括確認します。

最優先指摘である#1の挙動を正確に裏取りするため、`SanitizeFolderName`の実際の変換結果を検証します。

全ファイルを確認し、`SanitizeFolderName`の実挙動も実測しました。各指摘の裏取り結果です。

## 検証結果サマリ

凡例: ✅妥当 / 🔸一部妥当（誇張・例の誤り含む） / ❌非該当・誤り / ☑既に対処済み

---

### 🔴 セキュリティ

**#1 パストラバーサル — 🔸一部妥当（指摘の例は誤り、実害は限定的だが防御不足は本物）**

[DownloadViewModel.cs:301](YouTubeDownloader/ViewModels/DownloadViewModel.cs:301) と [SanitizeFolderName (494-502)](YouTubeDownloader/ViewModels/DownloadViewModel.cs:494) を実測検証しました:

| タイトル | サニタイズ後 | 解決先 |
|---|---|---|
| `..` | `..` | `C:\Users\X`（**親へ1段上昇＝実害あり**）|
| `../..`（評価の例）| `.._` | `…\Downloads\.._`（**移動しない＝例は誤り**）|
| `a/b` | `a_b` | 安全 |

- `SanitizeFolderName` は `GetInvalidFileNameChars()` を含むため、**`/` `\` は実は除去されます**（評価の「`/`が弾かれない」は誤り）。除去されないのは `.` のみ。
- したがって攻撃文字列を連結できず、**上昇できるのは1段だけ**。任意パスへの移動は不可。ただしタイトルが厳密に `..` の場合に親へ抜けるのは事実で、防御不足は実在します。
- 「[BuildOutputPath](YouTubeDownloader/Services/YtDlp/YtDlpOutputPathResolver.cs:11) のチェックは移動後のフォルダ基準なので突破される」という第2の指摘は**正しい**。`root` が既に親へ移動済みのため、テンプレート検証では検知できません。
- 評価の対策案（生成後に `GetFullPath` → 元 `SaveFolderPath` 配下か検証）は妥当。**結論: 優先度は評価ほど高くないが、修正する価値あり。**
- 🆕 おまけ: 予約デバイス名（`CON`等→`\\.\con`）も未対処でした。

**#2 Process.Start でシェル実行 — ✅妥当（ただし低リスク）**
[LibraryViewModel.cs:221-225](YouTubeDownloader/ViewModels/LibraryViewModel.cs:221)（`PlayFile`、`File.Exists`チェックあり）、[ExternalLinkOpener.cs:33-37](YouTubeDownloader/Infrastructure/ExternalLinkOpener.cs:33)（確認ダイアログあり）いずれも `UseShellExecute=true` で記述どおり。スキーム/拡張子検証は無し。指摘は正確です。

**#3 explorer.exe /select 引数インジェクション — ✅妥当**
[LibraryViewModel.cs:208](YouTubeDownloader/ViewModels/LibraryViewModel.cs:208) で `Arguments = $"/select,\"{item.LocalFilePath}\""` と文字列連結。`ArgumentList` 未使用で記述どおり。

---

### 🟠 並行性

**#4 Retry時のCTS競合 — ✅妥当（構造的に実在、低確率）**
[DownloadManager.cs:233](YouTubeDownloader/Services/DownloadManager.cs:233) の `finally` は `_cancellationTokens.Remove(job.Id)` を**無条件**で実行。一方 [Retry→ProcessJobAsync:159](YouTubeDownloader/Services/DownloadManager.cs:159) が新CTSを格納。Failed通知(227行)はロック外で発火するため、評価のシナリオどおり旧finallyが新CTSを消すレースが成立します。提案された「`current == cts` の時だけRemove」も標準的で正しい修正です。

**#5 AbsorbPermitAsync の fire-and-forget — ❌ほぼ非該当（誇張）**
[AbsorbPermitAsync:120-130](YouTubeDownloader/Services/DownloadManager.cs:120) は `WaitAsync()` のみ呼びます。`WaitAsync` がトークン無しで投げ得る例外は実質 `ObjectDisposedException` だけで、それは**既にcatch済み**。評価が挙げる `SemaphoreFullException` は `Release` 側の例外で、ここでは発生し得ません。`UnobservedTaskException` リスクは実質ゼロで、評価自身も「設計は正しく動く」と認めており、これは指摘として弱いです。

---

### 🟡 リソース

**#6 サムネイルキャッシュ無限成長 — ✅妥当**
[ThumbnailImageConverter.cs:20](YouTubeDownloader/Infrastructure/ThumbnailImageConverter.cs:20) `static Dictionary` に追加のみで削除なし。確認どおり（1枚は160px縮小デコードなので小さめだが、件数増で累積する）。

**#7 metadata.json 全件書き換え — ✅妥当**
[SaveAsync:66-70](YouTubeDownloader/Services/MetadataRepository.cs:66) が `_cache` 全体をシリアライズ、[SaveVideoMetadataAsync:89](YouTubeDownloader/Services/MetadataRepository.cs:89) が1件追加ごとに全書き込み。記述どおり。

**#8 _allJobs 無限成長 — ✅妥当**
[DownloadManager.cs:39](YouTubeDownloader/Services/DownloadManager.cs:39) のリストは `ClearCompleted`/`ClearAll` でのみ削減。[GetAllJobs:136](YouTubeDownloader/Services/DownloadManager.cs:136) は `ToArray()` コピー。記述どおり。

**#9 ViewModelのイベント未解除 — ✅妥当（潜在的）**
[DownloadViewModel.cs:44-45](YouTubeDownloader/ViewModels/DownloadViewModel.cs:44) と [:55](YouTubeDownloader/ViewModels/DownloadViewModel.cs:55) で購読、`Dispose`/`-=` なし。[MainWindowViewModel.cs:42,98](YouTubeDownloader/ViewModels/MainWindowViewModel.cs:42) で単一インスタンスにキャッシュされるため顕在化しない、という評価の但し書きも正確。

---

### 🔵 yt-dlp連携

**#10 --force-overwrites 無警告上書き — ✅妥当**
[YtDlpArgumentBuilder.cs:53](YouTubeDownloader/Services/YtDlp/YtDlpArgumentBuilder.cs:53) で常時付与。記述どおり。

**#11 User-Agent が古い — ✅妥当**
[YtDlpArgumentBuilder.cs:144](YouTubeDownloader/Services/YtDlp/YtDlpArgumentBuilder.cs:144) に `Chrome/120.0.0.0` がハードコード。記述どおり。

**#12 プレイリストURL判定が単純 — ✅妥当**
[YtDlpAnalyzer.cs:32](YouTubeDownloader/Services/YtDlp/YtDlpAnalyzer.cs:32) `url.Contains("list=") || url.Contains("/playlist")`。`watch?v=…&list=…` がプレイリスト扱いになるのは事実（空応答時のみ単一動画へフォールバック）。

**#13 stderr のANSI前提 — 🔸一部妥当（実装は妥当な選択）**
[YtDlpProcessRunner.cs:84](YouTubeDownloader/Services/YtDlp/YtDlpProcessRunner.cs:84) で `ANSICodePage` を使用。これは**リダイレクト時のPythonがロケール優先エンコーディングを使う挙動と整合**しており、英語OSなら cp1252 が選ばれ大半は問題になりません。`PYTHONIOENCODING`/UTF-8モードで崩れる余地はあるが、現状の選択は防御的で合理的。「文字化けし得る」は理論上正しいが実装は妥当な部類です。

---

### 🟢 機能の抜け穴（grep等で確認）

`cookie/proxy/limit-rate/write-sub` は**全コードでヒット0**でした。

| # | 検証 |
|---|---|
| 14 キュー非永続化 | ✅ `_allJobs` はメモリのみ。起動時「復元」(L58)も空配列で実質無効 |
| 15 Cookie/プロキシ未対応 | ✅ grep一致なし |
| 16 字幕未対応 | ✅ grep一致なし |
| 17 重複URLチェックなし | ✅ [Enqueue:140](YouTubeDownloader/Services/DownloadManager.cs:140) は無条件追加 |
| 18 リトライ回数制限なし | 🔸自動再試行は1回のみ([YtDlpDownloader.cs:122](YouTubeDownloader/Services/YtDlp/YtDlpDownloader.cs:122))。「無限」は**手動ボタン連打**の話で、深刻度は低い |
| 19 バッチURL不可 | ✅ 単一 `InputUrl` |
| 20 速度制限なし | ✅ grep一致なし |
| 21 個別品質指定不可 | ✅ [DownloadViewModel.cs:311](YouTubeDownloader/ViewModels/DownloadViewModel.cs:311) 全件 `SelectedQuality` |

---

### ⚪ その他

- **SettingsRepository.Load 同期I/O — ✅妥当** [SettingsRepository.cs:50](YouTubeDownloader/Services/SettingsRepository.cs:50) `File.ReadAllText`、コンストラクタ（UIスレッド）から呼ばれる。
- **AtomicFileWriter の directory空 — ✅妥当（防御的指摘）** [AtomicFileWriter.cs:18](YouTubeDownloader/Services/AtomicFileWriter.cs:18) に `directory ?? string.Empty` あり。実際は絶対パスのみで発生しない、という評価も正確。
- **CreateNoWindow — ☑既に設定済み（指摘は解消）** 評価は「コードが途中で切れて未確認」としていますが、[YtDlpProcessRunner.cs:60](YouTubeDownloader/Services/YtDlp/YtDlpProcessRunner.cs:60) に `CreateNoWindow = true` が**実在**。黒窓は出ません。

---

## 総評

21項目＋細目のうち、**大半（#2,3,4,6〜12,14〜17,19〜21 等）は正確**でした。一方で次は割り引いて読むべきです:

- **#1**: 脆弱性は実在するが、例示の `"../.."` は誤り（サニタイズで無効化）、実際は `..` 単体で**1段だけ**親へ抜ける限定的なもの。評価が言うほど最優先ではない。
- **#5**: ほぼ非該当。挙げられた `SemaphoreFullException` はこの経路で発生せず、唯一の例外は既にcatch済み。
- **#13**: 実装はPythonの挙動と整合した妥当な選択で、「問題」というより「エッジケース」。
- **CreateNoWindow**: 既に対処済み。

最も確実に直す価値があるのは評価の挙げる **#4（CTS競合）** で、これは構造的に正しい指摘です。#1は「最優先」ではないものの、`GetFullPath`での配下検証＋予約名対策を入れる価値はあります。
````