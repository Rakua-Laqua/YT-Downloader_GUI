<UserControl x:Class="YouTubeDownloader.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:YouTubeDownloader.ViewModels"
             xmlns:inf="clr-namespace:YouTubeDownloader.Infrastructure"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance Type=vm:LibraryViewModel}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <inf:ThumbnailImageConverter x:Key="ThumbToImage"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 検索バー -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="15" CornerRadius="8" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                         Padding="12,10" FontSize="14"
                         VerticalContentAlignment="Center">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
                <Button Grid.Column="1"
                        Content="検索"
                        Command="{Binding SearchCommand}"
                        Padding="20,10" Margin="10,0,0,0"
                        FontSize="14"/>
                <Button Grid.Column="2"
                        Content="更新"
                        Command="{Binding RefreshCommand}"
                        Padding="20,10" Margin="10,0,0,0"
                        FontSize="14"/>
            </Grid>
        </Border>

        <!-- ツールバー: 一括選択 / 一括削除 / 件数表示 -->
        <Grid Grid.Row="1" Margin="2,0,2,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="全選択"
                    Command="{Binding SelectAllCommand}"
                    Padding="14,7" FontSize="13"/>
            <Button Grid.Column="1"
                    Content="選択解除"
                    Command="{Binding ClearSelectionCommand}"
                    Padding="14,7" Margin="8,0,0,0" FontSize="13"/>
            <Button Grid.Column="2"
                    Content="選択削除"
                    Command="{Binding DeleteSelectedCommand}"
                    Padding="14,7" Margin="8,0,0,0" FontSize="13"
                    Foreground="Red"
                    ToolTip="選択した項目を履歴から削除します (Del)"/>

            <TextBlock Grid.Column="4"
                       Text="{Binding CountSummaryText}"
                       VerticalAlignment="Center"
                       FontSize="13" Foreground="#555"/>
        </Grid>

        <!-- ビジー表示 -->
        <Border Grid.Row="2" Background="#80000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"
                Panel.ZIndex="100">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding BusyMessage}" Foreground="White" FontSize="16"/>
            </StackPanel>
        </Border>

        <!-- ライブラリ一覧 -->
        <Border Grid.Row="2" Background="#F5F5F5" Padding="10" CornerRadius="8">
            <DataGrid ItemsSource="{Binding Items}"
                      SelectedItem="{Binding SelectedItem}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      Background="White"
                      BorderThickness="0"
                      RowHeight="60"
                      EnableRowVirtualization="True"
                      EnableColumnVirtualization="True"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      VirtualizingPanel.ScrollUnit="Pixel"
                      ScrollViewer.CanContentScroll="True">
                <DataGrid.InputBindings>
                    <!-- Del キーで選択(チェック)した項目を一括削除 -->
                    <KeyBinding Key="Delete" Command="{Binding DeleteSelectedCommand}"/>
                </DataGrid.InputBindings>
                <DataGrid.Columns>
                    <!-- 選択チェックボックス -->
                    <DataGridTemplateColumn Width="40" CanUserResize="False" CanUserReorder="False" CanUserSort="False">
                        <DataGridTemplateColumn.Header>
                            <CheckBox IsThreeState="True"
                                      HorizontalAlignment="Center"
                                      ToolTip="すべて選択 / 解除"
                                      IsChecked="{Binding DataContext.AreAllSelected, RelativeSource={RelativeSource AncestorType=DataGrid}, Mode=TwoWay}"/>
                        </DataGridTemplateColumn.Header>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox HorizontalAlignment="Center" VerticalAlignment="Center"
                                          IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- サムネイル -->
                    <DataGridTemplateColumn Header="" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="3" ClipToBounds="True"
                                        Background="#EEE" Width="70" Height="40" Margin="5">
                                    <Image Source="{Binding ThumbnailUrl, Converter={StaticResource ThumbToImage}}"
                                           Stretch="UniformToFill"
                                           RenderOptions.BitmapScalingMode="LowQuality"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- タイトル -->
                    <DataGridTemplateColumn Header="タイトル" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Title}"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"
                                           Margin="5,0"
                                           Foreground="Blue" TextDecorations="Underline" Cursor="Hand"
                                           ToolTip="{Binding Url}">
                                    <TextBlock.InputBindings>
                                        <MouseBinding MouseAction="LeftClick"
                                                      Command="{Binding DataContext.OpenVideoLinkCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                      CommandParameter="{Binding}"/>
                                    </TextBlock.InputBindings>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- チャンネル -->
                    <DataGridTextColumn Header="チャンネル" Binding="{Binding Channel}" Width="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                <Setter Property="Foreground" Value="Gray"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- 再生時間 -->
                    <DataGridTextColumn Header="時間" Binding="{Binding Duration}" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- フォーマット -->
                    <DataGridTextColumn Header="形式" Binding="{Binding Format}" Width="50">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- ダウンロード日時 -->
                    <DataGridTextColumn Header="ダウンロード日時" Binding="{Binding DownloadedAt}" Width="130">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="Gray"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- アクション -->
                    <DataGridTemplateColumn Header="" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="▶" ToolTip="再生"
                                            Command="{Binding DataContext.PlayFileCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Padding="8,4" Margin="2"/>
                                    <Button Content="📁" ToolTip="フォルダを開く"
                                            Command="{Binding DataContext.OpenFolderCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Padding="8,4" Margin="2"/>
                                    <Button Content="✕" ToolTip="履歴から削除"
                                            Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Padding="8,4" Margin="2"
                                            Foreground="Red"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- 空の状態 -->
        <TextBlock Grid.Row="2"
                   Text="ライブラリにアイテムがありません"
                   Foreground="Gray" FontSize="14"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}"/>
    </Grid>
</UserControl>
