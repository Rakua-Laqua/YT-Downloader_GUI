# CHANGELOG

## [3.9.3] - 2026-07-08

### 変更
- `.gitignore` に `docs/` および `tools/` ディレクトリを追加し、これらを Git の管理対象から除外しました。
- `tools/README.md` を Git の追跡から除外しました（ローカルファイルは保持）。
- `README.md` を更新し、cookies.txt を使用した認証についての解説の追加、プレイリスト内インデックスの説明修正、およびビルド・配布・リリース手順の変更を反映しました。
- 不要となった比較・評価関連のドキュメント（`比較/` および `評価/` ディレクトリ内の Markdown ファイル）を削除しました。

## [3.9.2] - 2026-07-02

### 追加
- `YtDlpDownloadException` 例外クラスを新設し、ダウンロード処理時のエラー原因や失敗詳細（FailureDetail）をより明確に伝播できるようにしました。
- `DialogPicker` 共通クラスを新設し、ファイルおよびフォルダの選択ダイアログ処理（OpenFileDialog, OpenFolderDialog）を集約・簡素化しました。
- `AppStorage` 共通クラスを新設し、ローカルアプリケーションデータフォルダのパス生成や、破損した設定・ライブラリ履歴ファイルの退避処理を集約しました。

### 修正
- `DownloadManager` において、ジョブ処理をバックグラウンドタスク（`Task.Run`）で開始することで、UIスレッドの同期的なブロックを防止しました。
- `ThumbnailImageConverter` において、画像のダウンロードやデコードが失敗した際に、メモリリークや不正な表示状態の維持を防ぐため、該当画像をキャッシュから自動的に削除するイベント処理を追加しました。

### 変更
- `YtDlpDownloader` および `YtDlpDownloadRunner` において、ダウンロードプロセスの出力解析を `DownloadOutputProcessor` インナークラスに抽出し、事前検証やフォールバック処理をメソッドへ分割することで、コードの可読性と保守性を向上させました。
- `MetadataRepository` および `SettingsRepository` から重複していたデータ保存先パス生成およびファイル退避のロジックを削除し、`AppStorage` クラスの共通メソッドを利用するようにリファクタリングしました。
- 設定画面 (`SettingsViewModel`) およびダウンロード画面 (`DownloadViewModel`) でダイアログを表示する箇所を、新設した `DialogPicker` を使用するように変更しました。

## v3.9.1 - 2026-07-01

### 変更
- `DownloadManager` における同時ダウンロード制限の実装を、`SemaphoreSlim` から `TaskCompletionSource` キューによる管理方式に変更し、動的上限変更時の挙動を安定化させました。
- `YtDlpDownloadRunner` において、出力ログ（stdout/stderr）のメモリ上限を設定し、キャンセル時にプロセスツリーを確実に強制終了させるリソースリーク・ハングアップ防止ロジックを追加しました。
- `YtDlpCookieProtector` に `ILoggingService` を渡し、一時コピー処理の状況をログ出力できるように改善しました。
- コードベース視覚化で生成された一時ファイル（`graph-data.js`, `graph-viewer.html`）を `.gitignore` の除外対象に追加しました。

## v3.9.0 - 2026-07-01

### 追加
- 設定・ライブラリ履歴ファイルの読み込みに失敗した際、不正なファイルを退避（`*.invalid-yyyyMMddHHmmss` 形式で自動バックアップ）し、デフォルト設定または空の履歴で自動復帰する機能を追加しました。
- `MetadataRepository` および `SettingsRepository` におけるファイル読み込み成否やバックアップ処理を日付別ログ（`LoggingService`）に出力するように改善しました。

### 修正
- 設定画面で自動検出された `yt-dlp` や `ffmpeg` のパスと、手動設定されたパスの不整合によって、自動検出されていてもパスが無効と判定されるバリデーションの不具合を修正しました。
- バリデーション処理と自動検出処理の競合による再帰的な不整合を解消しました。

## v3.8.0 - 2026-06-20

### 追加
- 設定画面に「認証 (cookie)」セクションを追加し、YouTubeログイン情報をエクスポートした `cookies.txt` のパスを指定できるようにしました（任意設定）。これにより、年齢制限・メンバー限定・非公開動画や、一時的なbot検知が発生した動画の取得・解析が可能になります。
- yt-dlpへのcookieファイル受け渡し時に、一時コピーファイルを介して実行し、実行後に一時ファイルを破棄する cookie 保護ロジックを追加しました。これにより、YouTube側のcookieローテーション書き戻しによるログイン失効やファイル破損を防ぎます。
- `*cookies.txt` などの認証・機密情報ファイルが誤ってGit管理に含まれないよう、`.gitignore` に除外パターンを追加しました。

## v3.7.0 - 2026-06-20

### 追加
- yt-dlpの標準エラー出力を解析し、日本語で原因と対処方法を提示するエラー分類機能を追加しました。
- 年齢制限、メンバー限定、非公開、bot検知、HTTP 429など、別clientで再試行しても回復しないエラーを検知した際、再試行を即座にスキップして無駄なアクセスによるIP制限の悪化を防ぐ機能を追加しました。

### 変更
- 初回ダウンロードおよび動画解析の段階から、安定性の高い `tv` clientを優先的に使用するように改善しました。これにより、デフォルトのclient（`web_safari` 等）で発生していたフォーマット数激減（SABR強制によるURL欠落）や初回失敗を回避します。
- ダウンロード失敗時の実行ログや詳細メッセージに、分類された推定原因と具体的な対処方法を表示するように変更しました。

### 削除
- 不要となった古いビルド・リリース用スクリプト（`publish.ps1`, `release.ps1`, `zip-source.ps1`）を削除しました。

## v3.6.1 - 2026-06-19

### 変更
- ダウンロード画面全体の外側スクロールを復活し、ダウンロードキュー部分も縦スクロールするようにしました。
- ソースアーカイブ作成時の既定出力先とファイル名を、実行時のカレントディレクトリ名に基づくようにしました。

## v3.6.0 - 2026-06-18

### 追加
- ダウンロード完了ジョブに情報アイコンを追加し、ホバーで所要時間、開始・完了時刻、動画長、要求形式、ファイルサイズ、保存先を確認できるようにしました。
- 完了情報ツールチップにフォーマット検証結果とyt-dlp選択情報、ffprobe実ファイル情報を表示するようにしました。

## v3.5.0 - 2026-06-18

### 追加
- ダウンロード完了後に、yt-dlpが選択したソースの拡張子・動画コーデック・音声コーデックと、ffprobeで取得した実ファイルの拡張子・動画コーデック・音声コーデックを比較するようにしました。
- フォーマット比較結果をログに出力し、不一致がある場合はダウンロードキューの完了ジョブに警告アイコンと詳細ツールチップを表示するようにしました。
- コーデック表記差（avc1/h264、av01/av1、mp4a/aacなど）を正規化して比較できるようにしました。
- 評価内容の検証資料と、フォーマット検証機能の実装案比較資料を追加しました。
- ソースアーカイブ作成用の `zip-source.ps1` を追加しました。

### 変更
- mp3/wavのように再エンコードが前提の音声変換は、コーデックやコンテナが変わっても警告ではなく情報として扱うようにしました。
- 生成されたソースアーカイブzipをGitの追跡対象外にしました。

## v3.4.1 - 2026-06-18

### 変更
- yt-dlp実行時の同名ファイル処理を `--no-overwrites` に変更し、既存ファイルを無警告で上書きしないようにしました。
- yt-dlpのUser-Agent指定を固定値から既定値に戻し、yt-dlp側の更新に追従するようにしました。

### 修正
- ジョブ再試行時に古いキャンセルトークンの後処理が新しいキャンセルトークンを削除し、再試行後のキャンセルが効かなくなる可能性を修正しました。
- 再試行処理でロック保持中にイベント通知とジョブ開始を行わないようにしました。
- プレイリスト名から保存フォルダーを作る際に、`.` / `..` / Windows予約名を安全なフォルダー名へ置き換えるようにしました。
- ライブラリから開く外部URLを http / https に限定し、不正なスキームをシェル実行しないようにしました。
- ライブラリのファイル再生を対応メディア拡張子に限定し、フォルダー表示では `ArgumentList` を使ってexplorer引数を渡すようにしました。
- サムネイル画像キャッシュに上限を設け、ライブラリ件数が増えた場合のメモリ増加を抑えるようにしました。
- プレイリストURL判定をURIパースに変更し、URL全体の単純な部分一致による誤判定を防ぐようにしました。

## v3.4.0 - 2026-06-16

### 追加
- yt-dlpのstdout要約と診断用stdoutを保持し、ダウンロード失敗詳細とログで確認できるようにしました。
- READMEに機能一覧、必要条件、設定、保存データ、配布ビルド、リリース手順の説明を追加しました。

### 変更
- yt-dlpの実行ログに、出力先、マージ、音声抽出、ファイル移動などの主要なstdout行を要約として出力するようにしました。
- stderrに明確なエラーがない場合は、stdout診断行からリトライ、HTTPエラー、タイムアウト、fragment関連の情報をエラー要約に使うようにしました。

### 修正
- 初回ダウンロード失敗後にフォールバック再試行で成功した場合、初回試行で残った可能性があるyt-dlp中間ファイルを削除するようにしました。

## v3.3.1 - 2026-06-16

### 変更
- `YtDlpClient` に集中していた解析、更新、ダウンロード、引数構築、プロセス実行、失敗詳細整形の処理を責務別クラスへ分割しました。
- `IYtDlpClient` の外向きの契約を維持したまま、yt-dlp連携処理の保守性を改善しました。

## v3.3.0 - 2026-06-16

### 追加
- アプリの実行ログを `%LocalAppData%\YouTubeDownloader\logs` へ日付別に出力する `LoggingService` を追加しました。
- ダウンロード失敗時に、失敗フェーズ・終了コード・実行コマンド・stderr全文などを保持し、ダウンロード画面からコピーできるようにしました。

### 変更
- yt-dlpの初回試行とフォールバック再試行について、失敗フェーズ・経過時間・yt-dlpバージョンをログと失敗詳細に記録するようにしました。
- ジョブ開始、キャンセル、成功、失敗、yt-dlp自動更新失敗をログへ記録するようにしました。

### 修正
- ファイル名テンプレートや完了後ファイル探索で、`Ep.01` のような末尾の数値を拡張子として誤判定しないようにしました。

## v3.2.2 - 2026-06-16

### 変更
- ダウンロード進捗表示で、動画データと音声データのダウンロード中ステータスを区別するようにしました。
- マージ処理中のステータス表示を、変換中からマージ中に変更しました。

### 修正
- yt-dlpの `--js-runtimes` をランタイムごとに指定し、DenoとNode.jsへのフォールバックが正しく効くようにしました。

## v3.2.1 - 2026-06-16

### 修正
- `release.ps1` の既定asset検出で、過去バージョンの配布ファイルまでアップロード対象に含まれる問題を修正しました。

## v3.2.0 - 2026-06-16

### 追加
- GitHub Releaseの作成、タグpush、配布ファイルアップロードを行う汎用 `release.ps1` を追加しました。
- yt-dlp実行時にJSチャレンジ解決用ランタイムとして `deno,node` を指定するようにしました。

### 変更
- ダウンロード完了後のローカルファイルパス特定を、yt-dlpの `after_move` 出力を優先して使用する方式に変更しました。
- ダウンロード失敗時に、初回試行とフォールバック再試行のエラー内容を分けて表示するようにしました。

### 修正
- ファイル更新日時を公開日に合わせる処理で、実際に移動完了した最終ファイルへより確実に反映できるようにしました。

## v3.1.1 - 2026-06-16

### 修正
- ファイル更新日時を公開日に合わせる設定で、yt-dlpの `timestamp` が取得できる場合は公開時刻まで反映するようにしました。
- 公開時刻をUTC絶対時刻として保存し、閲覧環境のローカル時刻で正しく表示されるようにしました。
- 設定画面の説明文を公開時刻まで反映する挙動に合わせて更新しました。

## v3.1.0 - 2026-06-16

### 追加
- ダウンロード後にファイルの更新日時を動画の公開日に合わせる設定を追加しました。
- 埋め込みメタデータの「年」タグを動画の公開年に修正する設定を追加しました。

### 変更
- 設定保存後の既定保存先変更を、次回URL解析時のダウンロード画面に反映するようにしました。

## v3.0.3 - 2026-06-16

### 変更
- yt-dlp呼び出しを文字列連結から `ArgumentList` ベースに変更し、URLやパスに含まれる特殊文字で引数が崩れないようにしました。
- 設定・メタデータJSONの保存を一時ファイル経由の原子的な差し替えに変更しました。
- ビルド後コピー先を既定では空にし、必要な場合だけ `PostBuildCopyOutputDir` で指定する方式にしました。

### 修正
- 解析・更新・バージョン取得のyt-dlpプロセスがキャンセル時に残る可能性を修正しました。
- ファイル名テンプレートで保存先フォルダー外を指せてしまう問題を修正しました。
- ダウンロード完了後のローカルファイル特定で付随ファイルを拾う可能性を低減しました。
- READMEのビルドコマンドが実行できない問題を修正しました。
- 未使用のダウンロードキュー定義と到達不能な進捗解析分岐を整理しました。

## v3.0.2 - 2026-06-15

### 変更
- タイトル取得言語の既定値を `default` に変更し、YouTube動画の主言語メタデータを優先するようにしました。
- 設定画面のタイトル取得言語候補で `default` を先頭に表示し、説明文を実際の挙動に合わせて更新しました。

### 修正
- タイトル取得言語が空欄または不正値の場合に `ja` を強制し、英語などの動画タイトルが日本語翻訳タイトルで保存される問題を修正しました。

## v3.0.1 - 2026-06-15

### 変更
- yt-dlpの解析・ダウンロード失敗時に、tv / web_safari / android clientで再試行するようにしました。
- yt-dlpの標準エラー出力をシステムANSIコードページでデコードし、日本語エラーの文字化けを抑えるようにしました。

### 修正
- 動画情報取得失敗時に汎用メッセージだけでなくyt-dlpの実際のERROR行を表示するようにしました。
- ダウンロード失敗時のエラー表示をERROR行中心に整理し、キューのステータス表示を折り返しとツールチップで確認しやすくしました。

## v3 - 2026-06-15

### 追加
- yt-dlpの更新チャンネル（stable/nightly）を設定画面で選択できるようにしました。
- 現在インストールされているyt-dlpのバージョン表示を追加しました。
- プレイリスト解析でYouTube上の総数と取得件数を比較し、取得漏れがある場合に警告を表示するようにしました。
- 設定画面のダウンロード設定に、同時ダウンロード数（1〜8）の選択を追加しました。

### 変更
- yt-dlp更新処理を `--update-to <channel>` に変更し、stable/nightly間の切り替えに対応しました。
- 自動更新時も保存済みの更新チャンネルを反映するようにしました。
- ダウンロード管理を `MaxConcurrentDownloads` 設定と連動させ、保存後の同時ダウンロード数変更を実行中ジョブを止めずに反映するようにしました。
- プロジェクト全体処理資料を現状の実装に合わせて更新しました。

### 修正
- `--update-to` の出力で更新済み判定と最新判定が逆転しないようにしました。
- 古いyt-dlpでプレイリスト取得件数が不足した場合に気付きにくい問題を改善しました。
- `MaxConcurrentDownloads` が設定モデルにあるだけで実際の並列数に反映されない問題を修正しました。

## v2 - 2026-06-14

### 追加
- 音声ダウンロードの品質プリセットを追加し、設定画面とダウンロード画面で選択できるようにしました。
- ライブラリで全選択、選択解除、選択削除、件数表示を追加しました。
- サムネイル画像を縮小デコードしてキャッシュするコンバーターを追加しました。
- ビルド後に指定フォルダーへ成果物をコピーする設定を追加しました。
- 処理フローと評価・比較資料を追加しました。
- プロジェクト全体処理を3段階で整理した資料を追加しました。

### 変更
- mp3/wav変換時にffmpegの進捗を表示し、変換・メタデータ書き込みなどの後処理ステータスを表示するようにしました。
- m4aではAAC音源を優先し、再エンコードを避けやすい形式選択へ変更しました。
- 設定保存後の既定フォーマット・品質を次回URL解析時に反映するようにしました。
- ライブラリ検索を入力デバウンス付きのライブ検索へ変更し、一覧の再読み込みを直列化しました。
- ライブラリ一覧の仮想化とサムネイル読み込みを改善しました。
- ナビゲーション選択の同期方式を見直し、ライブラリ表示時の読み込みを確実に実行するようにしました。
- yt-dlpの進捗解析とJSONプロパティ取得をより堅牢にしました。
- プロジェクト処理フロー資料の起動・更新フロー説明を実装に合わせて更新しました。

### 修正
- ダウンロードのキャンセル時にyt-dlp/ffmpegプロセスが残る問題を修正しました。
- 待機中のジョブをキャンセルしてもセマフォ取得後に実行される問題を修正しました。
- 保存先フォルダーを作成できない場合にアプリがクラッシュしないようにしました。

## v1 - 2026-06-15

### 追加
- mp4保存時にAV1などの高効率コーデックを優先する設定を追加しました。

### 変更
- mp4形式の品質指定時に、高効率コーデック優先設定を反映する形式選択へ変更しました。
